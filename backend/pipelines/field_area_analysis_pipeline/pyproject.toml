[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "field-area-analysis-pipeline"
version = "0.1.0"
description = "Spatial analysis pipeline for agricultural fields"
authors = [{name = "Landbrugsdata Team"}]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "duckdb>=1.3.0",
    "pandas>=2.2.0",
    "gcsfs>=2024.6.0",
    # Parquet support (CRITICAL - was missing)
    "pyarrow>=16.1.0",
    # Additional utilities
    "python-dotenv>=1.0.1",
    "tqdm>=4.66.2",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "black>=23.0.0",
    "ruff>=0.1.0",
]

[project.scripts]
field-analysis = "field_analysis_pipeline.main:main"

[tool.hatch.build.targets.wheel]
packages = [".", "bronze", "silver"]
include = ["README.md", "*.py"]

[tool.hatch.build]
only-packages = true
artifacts = ["*.py"]

[tool.uv]
# Optimize for geospatial dependencies

[tool.black]
line-length = 100
target-version = ['py311']

[tool.ruff]
line-length = 100
target-version = "py311"
select = ["E", "F", "I", "N", "W"]
ignore = ["E501"]  # Line too long (handled by black)

[tool.ruff.isort]
known-first-party = ["field_area_analysis_pipeline"]

[tool.ruff.per-file-ignores]
"__init__.py" = ["F401"]

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
disallow_untyped_decorators = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
warn_unreachable = true
strict_equality = true

[[tool.mypy.overrides]]
module = [
    "duckdb.*",
    "geopandas.*",
    "shapely.*",
]
ignore_missing_imports = true 