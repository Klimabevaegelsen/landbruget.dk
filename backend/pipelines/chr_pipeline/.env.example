# FVM Credentials (required)
FVM_USERNAME=your_username
FVM_PASSWORD=your_password

# VetStat Certificate (required)
VETSTAT_CERTIFICATE_PASSWORD=your_certificate_password

# Optional: Cloud Storage (for production)
# STORAGE_BUCKET=landbruget-data
# GOOGLE_CLOUD_PROJECT=your-project-id
# GCS_BUCKET=your-bucket-name  # Legacy fallback
